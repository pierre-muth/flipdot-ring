> Electronic
  - done. Insert a general fuse directly after BMS. Put the general switch to a small proto-board, remove connectors to BMS.
  - done. Use Pololu step-up/down modules for 5v and 24v
  - 2 boards standby current is 1.4 + 0.9 mA. 
  - If standby current too high, test shuting down 24v step-up during deep sleep, resistor for pull-up or pull-down needed ?
  - If standby current too high, put a small battery on the Xiao module and test shuting down 5v step-down during sleep. Pull-up/down ? 
    . If switching off 5v, how the PIC react ? 
    . Is enough to charge the Xiao battery ?

> PIC code
  - Use the FP power configuration bit to manage the PIC SLEEP mode. Prevent sleep might be useful for Demo animations
  - Start the latch timer on IOC interrupt. Hoping to reject the wrong edges generated by the ESP32 at wakeup.
    . At latch time, check if the number of byte recieved is %24 and if last config byte is 0xAA.

> ESP32 code
  - At display data generation, in addition of config bits, generate a 0xAA byte on the lasts bit[7].
  - Measure the main battery voltage with ADC (before switching on the voltage regulators, if effective?)
    . Use voltage divider.
    . If main battery is dead, deep sleep for a long period. Keep DC/DCs off if we use 'enable' pin and, will need a small Xiao battery.
    . If main battery is almost dead, print a message
    . If main battery is 25% refresh only every 10 min
  - ESP32 button ?
      . Is it waking up from sleep
      . If button pressed, alternate different modes. Hot spot setup, clock face 1, 2, 3, demo mode ?
  - Night time interval. Setup start / end hours. Night time ideas:
      . Refresh only eveny 10 min
      . Flip points only one by one
