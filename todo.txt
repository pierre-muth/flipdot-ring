> Electronic
  - done. Insert a general fuse directly after BMS. Put the general switch to a small proto-board, remove connectors to BMS.
  - done. Use Pololu step-up/down modules for 5v and 24v
  - done. Make a voltage divider for battery measurement.
  - 2 Pololu boards standby current is 1.4 + 0.9 mA. 
  - If standby current consumption is too high, likely not needed.
    . test shutting down 24v step-up during deep sleep, seems pullup at Vbatt!
    . put a small battery on the Xiao module and test shuting down 5v step-down during sleep. Pull-up/down ? 
    . If switching off 5v, how the PIC react ? 
    . Is enough uptime to charge the Xiao battery ?

> PIC code
  - untested. Use the FP power configuration bit to manage the PIC SLEEP mode. Prevent sleep might be useful for Demo animations
  - done. Start the latch timer on IOC interrupt. Hoping to reject the wrong edges generated by the ESP32 at wakeup
  - done. At latch time, check if the number of byte recieved is %24 and if last config byte is 0xAA.

> ESP32 code
  - done. At display data generation, in addition of config bits, generate a 0xAA byte on the lasts bit[7].
  - Measure the main battery voltage with ADC
    . done. If main battery is dead, deep sleep for a long period and display a message.
    . If main battery is 25% refresh only every 10 min
  - ESP32 button, press to ground.
    . done. ESP32 pin in pullup even in deep sleep.
    . done. it wakes up from sleep and set in configuration mode.
    . done. if button pressed a second time, launch demo mode.
    . different watch faces ?
  - Night time.
    . done. Refresh only eveny 10 min
    . Flip points only one by one
    . Start and stop of night time in the config webpage.
